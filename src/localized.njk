---
title: Daily localized Breifing
layout: base.njk
pagination:
        data: news
        size: 1
        alias: news
permalink: /{{ news.country }}/index.html
---

<div class="container">
        This is a page for you
        {% for article in news.articles %}
        <ul>
        <li><h2><a href="{{article.url}}">{{ article.title }}</a></h2></li>
        <span>{{ article.source.name }} - <time>{{article.publishedAt}}
</time></span>        </ul>
        {% endfor %}
</div>
<script src="/js/location.js"></script>